name: QIMEN_MARKET_001 Historical Exploration

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.github/workflows/research-qimen-market.yml'

permissions:
  contents: read

jobs:
  research:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install research dependencies
        run: pip install -e '.[data]'
      - name: Run QIMEN_MARKET_001 on pinned Sina history
        run: >-
          python -m metaalpha.research_qimen_market
          --fetch-akshare
          --provider sina
          --symbol 000001
          --start 19901219
          --end 20260814
          --out reports/qimen_market_exploratory
      - name: Show evidence status and provenance
        if: success()
        run: |
          cat reports/qimen_market_exploratory/SUMMARY.md
          cat reports/qimen_market_exploratory/run_metadata.json
          cat reports/qimen_market_exploratory/data_manifest.json
          cat reports/qimen_market_exploratory/invalid_inference_tests.csv
      - name: Upload research artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: qimen-market-${{ github.sha }}
          path: reports/qimen_market_exploratory
          if-no-files-found: warn
          retention-days: 30
